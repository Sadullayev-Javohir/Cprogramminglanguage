#include <stdio.h>

int main()
{
  // int n;
  // printf("Massiv uzunligini kiriting: ");
  // scanf("%d", &n);

  // int arr[n];

  // printf("Massivlarni kiriting: \n");
  // for (int i = 0; i < n; i++)
  // {
  //   printf("[%d] = ", i);
  //   scanf("%d", &arr[i]);
  // }

  // // 2 baravarga oshirish

  // int *p = arr;
  // for (int i = 0; i < n; i++)
  // {
  //   *(p + i) = *(p + i) * 2;
  // }

  // printf("Natijasi: \n");
  // for (int i = 0; i < n; i++)
  // {
  //   printf("[%d] = %d\n", i, arr[i]);
  // }
}
